apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-data
  namespace: transmission
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ""
  volumeName: transmission-data
