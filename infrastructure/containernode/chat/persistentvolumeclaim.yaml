apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chat-data
  namespace: chat
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeName: chat-data
