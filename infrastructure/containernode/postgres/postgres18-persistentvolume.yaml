apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres18-data
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /mnt/userdata-clusterfiles/k3s-volumes/postgres18
    type: DirectoryOrCreate
  claimRef:
    namespace: postgres
    name: postgres18-data
