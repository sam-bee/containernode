# Research tools

- `exec` is routed through OpenClaw to the paired node named `research`.
- Host-side approval policy is authoritative even when a tool appears in the model interface.
- Initially only `/usr/bin/uname` is pre-approved for installation testing.
- Never try to reach the research host by another route or bypass command approval.
